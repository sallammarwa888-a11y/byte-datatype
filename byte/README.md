# Byte

A simple unsigned 8-bit integer datatype for Python.

"
from byte import Byte

x = Byte(250)
print(x + 10)  # Byte(4)
"

install with:
"pip install byte-datatype"

the link to repository:
https://github.com/sallammarwa888-a11y/byte-datatype.git