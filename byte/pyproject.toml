[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "byte-datatype"
version = "1.0.0"
description = "A Byte datatype for Python."
authors = [
    {name = "Your Name"}
]
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"